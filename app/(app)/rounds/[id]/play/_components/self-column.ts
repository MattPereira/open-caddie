// Shared by the header, hole rows and the Out/In totals so the signed-in
// player's column reads as one continuous band. Deliberately the same fill as
// the next-hole row, so the two cues read as one language at the same weight.
export const selfColumnClassName = "bg-muted/50";
